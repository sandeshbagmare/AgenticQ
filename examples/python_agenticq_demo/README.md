# Python AgenticQ Demo

Small FastAPI service used to validate AgenticQ scanning, recommendation, and scaffolding.

## Endpoints

- `GET /health` -> basic health response
- `GET /items/{item_id}` -> echo item id
