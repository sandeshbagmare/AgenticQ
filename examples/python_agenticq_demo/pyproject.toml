[project]
name = "python-agenticq-demo"
version = "0.1.0"
description = "Demo FastAPI service for AgenticQ testing."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
